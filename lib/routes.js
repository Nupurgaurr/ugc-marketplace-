/**
 * Single place the two pathways are addressed from.
 *
 * Deployed (Vercel / `next dev`) paths. If you ever go back to a static export
 * opened straight from disk, switch these to './find-a-creator.html' etc.
 */
export const ROUTES = {
  home: '/',
  findCreator: '/find-a-creator',
  becomeCreator: '/become-a-creator',
};
